deployment:
  production:
    branch: master
    commands:
      - "git push git@heroku.com:as-lion.git HEAD:master"
      - "heroku run rake db:migrate --app as-lion"
      - "heroku restart --app as-lion"
